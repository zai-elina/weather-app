export const weatherTodaySelector = (state) => state.weatherData.today;

export const weatherForWeekSelector = (state) => state.weatherData.weekly;

export const weatherForHoursSelector = (state) => state.weatherData.hourly;
